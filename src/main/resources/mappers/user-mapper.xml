<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.buddyguard.bodyguard.repository.UserRepository">

    <insert id="save" parameterType="user">
        insert into
            users(email, password, nickname, image_url, provider, provider_id, verified, created_at)
        values
            (#{email}, #{password}, #{nickname}, #{imageUrl}, #{provider}, #{providerId}, #{verified}, now() )
    </insert>

    <select id="findByEmail" resultType="user">
        select * from users where email = #{email}
    </select>

    <update id="updateUserProfileByEmail">
        update users set nickname = #{request.nickname}, image_url = #{request.imageUrl},
        gender = #{request.gender}, height = #{request.height},
        weight = #{request.weight}, age = #{request.age}, goal = #{request.goal},
        info_public = #{request.infoPublic}
        where email = #{email}
    </update>

</mapper>